#include "main.h"
/**
 *_abs - Check Main
 * @r: An integer input
 * Description: This function returns the abs  of a number
 * Return: Absolut value of number r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
